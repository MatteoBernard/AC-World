import ACText from "@/components/ACText";
import ACView from "@/components/ACView";

const Memo = () => {
    return (
        <ACView>
            <ACText>Memo</ACText>
        </ACView>
    );
}

export default Memo;